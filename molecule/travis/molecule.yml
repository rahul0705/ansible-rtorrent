---
dependency:
  name: galaxy
driver:
  name: delegated
  options:
    ansible_connection_options:
      connection: local
lint:
  name: yamllint
platforms:
  - name: localhost
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: travis
verifier:
  name: testinfra
  lint:
    name: flake8
